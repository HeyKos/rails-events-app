- title "Events Listing"
%h1 Events
= link_to 'New event', new_event_path
%table.table.table-data
  %thead
    %tr
      %th Id
      %th Title
      %th Location
      %th Occurs On
  %tbody
  - @events.each do |e|
    %tr
      %td= e.id
      %td= e.title
      %td= e.location
      %td= e.occurs_on
